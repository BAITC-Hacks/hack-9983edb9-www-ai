# Optional: simulation works without AI.
OPENAI_API_KEY=
# This file is a template only. The application does NOT automatically load .env.
# Set OPENAI_API_KEY in the backend process environment before starting it.
# Never commit real API keys or place them in frontend code.
